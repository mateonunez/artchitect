apiVersion: apps/v1
kind: Deployment
metadata:
  name: archivel-laravel
  labels:
    app: archivel-laravel
spec:
  replicas: 1
  selector:
    matchLabels:
      app: archivel-laravel
  template:
    metadata:
      labels:
        app: archivel-laravel
    spec:
      containers:
        - name: archivel-laravel
          image: mateonunez/architect-backend-php-laravel:1.2.4
          ports:
            - containerPort: 9000
          env:
            - name: DB_HOST
              valueFrom:
                configMapKeyRef:
                  name: archivel-mysql-configmap
                  key: endpoint
---
apiVersion: v1
kind: Service
metadata:
  name: archivel-laravel-service
spec:
  selector:
    app: archivel-laravel
  ports:
    - protocol: TCP
      port: 9000
      targetPort: 9000
